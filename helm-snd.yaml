
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

ingress:
  enabled: true
  hosts:
    - host: "course-unlocker-snd.apps-snd.iu.edu"
      paths: []

activeProfiles: "snd"

serviceAccountName: course-unlocker-snd

cluster: es-test